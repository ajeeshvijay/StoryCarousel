class StoryCarousel {

    constructor(element, opts) {
        this.element = element;
        this.opts = opts;
        this.init()
    }

    init() {
        console.log(`Options: ${this.opts}`);
    }
}